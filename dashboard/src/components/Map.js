import React from "react";

export default function Map({ complaints }) {
  return (
    <div style={{ marginTop: 20 }}>
      <h2>Map placeholder</h2>
      <p>(For demo, we show location values in table. Add Google Maps later.)</p>
    </div>
  );
}
